# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
target/

# Tauri
src-tauri/target/
src-tauri/gen/

# Go engine build artifacts (binaries are committed under src-tauri/bin)
services/pdf-engine/pdflexity-engine
services/pdf-engine/pdflexity-engine.exe

# OCR feature-pack build artifacts (built on demand / by CI, hosted as a
# GitHub release asset — never committed; multi-hundred-MB)
services/ocr-engine/.venv*/
services/ocr-engine/.build/
src-tauri/resources/

# Env / logs
.env
.env.local
*.log
